---
name: serie-modelo-dados-f0-06
description: "Estado da série de incrementos de modelagem de dados (F0-06, 00–09) do systeme-erp"
metadata: 
  node_type: memory
  type: project
  originSessionId: c1de9174-b313-439d-aa7a-75342c5b6026
  modified: 2026-09-10T11:28:57.036Z
---

A série F0-06 modela os dados do ERP em 10 incrementos SDD (`00-modelo-dados-fundacao` … `09-modelo-dados-plataforma`), documental, sem código. Cada um: branch própria + PR. Repo: `systeme-dev-br` (ver [[repos-github-systeme]]).

**SÉRIE F0-06 100% ENCERRADA em 2026-09-10 — os 10 incrementos (00–09) consolidados em `main`.** Contratos vivos: `sdd/contratos/modelo-dados-{fundacao,multiempresa,cadastros,suprimentos,vendas,fiscal,financeiro,pdv,crm,plataforma}/contrato.md` + `sdd/contratos/dados/contrato.md`. Docs `docs/arquitetura/modelo-dados/00..09-*.md`. `sdd/metricas.csv` com 00–09 (12 col). `main` em `de33cd2` (merge #190). `sdd/incrementos/` vazio (tudo em `sdd/historico/2026-09-05-NN-*/`). **Faxina de branches concluída em 2026-09-10:** todas as branches `sdd/*` da série (00–09, ~26 no total, local+remoto) apagadas após confirmar que estão em `main`. Repo com zero branches `sdd/`. **Retrospectiva:** 2 bugs de QA em 10 ciclos (02, 04 — P2, corrigidos no mesmo ciclo), 0 P0/P1 em qualquer ciclo, reauditorias de 4 (00) para 0–1 nos últimos quatro, impl. estável 1–2h desde o 04. As fases F1+ (migrations/seed/rollback/runtime) consomem estes 10 contratos como base. Patches de prompt acumulados na série: 01 (ponteiros de ref cruzada + premissas prospectivas), 02 (`empresa_id` transversal), 04 (`pre-implement task_01` na auditoria; NÃO editar `execucao.md`), 06 (`empresa_id` nas tabelas-detalhe; contagem `public` bate; reconciliar contagens/universais em todo o doc), 07/08 (não escrever literais `-XXX`/`NAO_VERIFICADO` nos relatórios), 13 (`git add` do yaml antes do `git mv`; `metricas.csv` fora da PR), 14 (`metricas.csv` na PR do 14 com backlog), 00 (verificar cadeia de gates ao subir autonomia; pré-requisitos de infra em `main` antes da base). Runbooks: `sdd/governanca/runbook-recuperacao-git.md`, `runbook-merge-sdd.md`.

---
**Histórico da execução (arquivado) — estado em 2026-09-09 (00–08 FECHADOS; 09-plataforma EM ESPECIFICAÇÃO — último da série):**
- **08-crm:** **FECHADO — ciclo SDD 14/14.** 3 PRs mescladas: **#184** modelo (`5b5f87b`), **#185** consolidação (`6c1920a` — `main` fica aqui), **#186** aprendizados (`1ba19a8`). `main` tem `docs/arquitetura/modelo-dados/08-crm.md` (802 linhas, §1–§21, ~48 entidades `crm_*` — todas `tenant_*`, `public` = zero), `sdd/contratos/modelo-dados-crm/` (NOVO — 6 comportamentos) + `dados/` (ALTERADO), `sdd/aprendizados/2026-09-09-08-modelo-dados-crm.md`, `sdd/governanca/runbook-recuperacao-git.md`, patches dos prompts 01/02, `sdd/metricas.csv` 00–08. Arquivado em `sdd/historico/2026-09-05-08-modelo-dados-crm/`. **3º ciclo limpo seguido** (0 reauditoria / 0 revisão de plano / 0 bug / nenhum P2; 2 P3 de auditoria + 2 P3 de review aceitos como dívida). Incidente de `.git` corrompido (19 objetos zero-byte) no meio do ciclo, **recuperado sem perda** porque todo commit SDD é empurrado na hora — recipe no runbook. Métricas: espec 96h, impl 2h, reauditorias 0, revisões de plano 0, issues 2, lead 118h. Patches: prompt 01 (reconciliar TODO ponteiro parentético de ref cruzada com o título real da seção + checklist; premissas prospectivas do PRD não colidem com PRM-010/011 do yaml); prompt 02 (bloco de modelagem por tenant → enunciado transversal "`<prefixo>_*` carrega `empresa_id`" repetido nas tabelas-detalhe). **Autonomia elevada:** dono aprovou `autonomo_ate_pr` para a mecânica documental (passos 06–10) do **09**, apoiado nos 3 ciclos limpos; passos 00–03 e gates de especificação/PR/merge seguem sob aprovação humana.
- **09-plataforma (EM ESPECIFICAÇÃO):** branch `sdd/09-modelo-dados-plataforma-plan` (de `main` `6c1920a`, empurrada). `rigor: large`, `risco: medio`, **`autonomia: autonomo_ate_pr`**, `alvo: branch`. Domínios `modelo-dados-plataforma` (NOVO) + `dados` (ALTERADO). 7 módulos: **BILL** (no `public` por `conta_id`), **LGPD**, **NOT**, **API**, **IMPEX**, **BI**, **MOB**. Depende de 00–08 (todos consolidados). `gates.especificacao.gate_humano.requerido: true` (matriz mínima: autonomia > `assistido` + billing/LGPD). Commits: `289f4c5` base (base_sha `6c1920a`; contratos_lidos = 9 contratos vivos + docs 00–08; PRM-009/010/011); `bd42bb3` passo 01 (revisão do PRD — premissas renumeradas p/ não colidir, ponteiros de ref cruzada reconciliados); `a7a0dea` passo 02 (`_techspec.md` ~600 linhas, 7 blocos, ~79 entidades + `adrs/adr-001..005`: 001 billing no `public`/conta = unidade de cobrança; 002 trilha de auditoria completa por extensão de vocabulário, sem coluna nova nos blocos 01–08; 003 publicação única de evento/API porta externa; 004 BI leitura agregada sem marts; 005 fila do MOB própria distinta da do PDV/07); **`8c01bed` passo 03** (`INDEX.md` + `task_01..05` [BILL/LGPD/NOT+API/IMPEX/BI+MOB+FRONTEIRAS] + `feature/001__plataforma-bill-lgpd-not-api-impex-bi-mob.feature` FEAT-001..005/SCN-001..010 + `impacto-contratual/{modelo-dados-plataforma (NOVO, 8 comportamentos), dados (ALTERADO)}` + `execucao.md` base `6c1920a`, RSK-001..008, gate humano do plano REQUERIDO). `compozy tasks validate` → `all tasks valid (5 scanned)`; `compozy sync` OK; `scan-secrets` OK. `fase: especificacao → planejamento`. Fronteira `public`: BILL + catálogos normativos globais (`plat_catalogo_eventos`, `impex_modelos_planilha`) sob a regra genérica dos 5 critérios (fecha QA-1 do PRD-02 e QA-5 do PRD-05); resto em `tenant_*`. Entregável: `docs/arquitetura/modelo-dados/09-plataforma.md`. **Passo 04 auditoria FEITA** (`cz-auditor-especificacao`, commits `5a00128` correção material + `b36dbad` relatório): parecer **PRONTO**, 0 P0/P1. **Evidence SHA `5a00128`**. AUD-001 (P2, autonomia `assistido` vs `autonomo_ate_pr` no `_prd.md`) + AUD-002 (P3, PRM-005 divergente) corrigidos; AUD-003 (P3, `empresa_id` transversal por tabela) delegado à redação passos 06/07. `status: especificado`, `fase: auditoria`, `reauditorias: 0`. **Gate humano de especificação: dono APROVOU** (AskUserQuestion 2026-09-09, "seguir sob autonomo_ate_pr"). **BLOQUEIO de infra encontrado:** o `pre-implement` chama o authority checker (`/usr/local/bin/sdd-authority-check`, SHA fixado em `policies.yaml`) com gate `especificacao`; o checker do AG-001 opção A foi escrito quando a série era toda `assistido` (gate sempre dispensado, caminho `else` sem authority check) e NEGA `especificacao` por não enumerá-lo — 09 é o 1º incremento com autonomia > `assistido`, e o `sdd-guard.sh:1349` força `requerido: true` + authority check nesse nível. **Dono escolheu opção A: fechar a lacuna.** Feito: `sdd/governanca/authority-check.sh` (`especificacao` na allowlist de gates de fluxo) + `policies.yaml` (`check_sha256` → `bed5d72d628339f81932d5d2103fe624e70ee8ca61761e93f78288c6811c6fb7`); `/usr/local/bin/sdd-authority-check` reinstalado localmente (dono rodou o `sudo install`, SHA confere). **PR #187 ABERTA** (branch `sdd/authority-checker-especificacao` de `origin/main` `1ba19a8`, commit `c1a3980`) — `guard` vermelho esperado (trust root), **aguarda merge manual do dono**. A mudança de governança NÃO pode ficar no working tree do 09 (`validate_audit_evidence`: qualquer arquivo != evidência de entrega alterado desde o Evidence SHA → "reaudite"). **#187 MESCLADA** (`main` `dc60db3`, `c1a3980` no histórico). Sequência executada: rebase de `sdd/09-modelo-dados-plataforma-plan` sobre `dc60db3` (limpo), `base_sha → dc60db31c707a6b072a86253c349b5edc97fb644`, aprovação do gate de especificação reaplicada (`99a0747`), **reauditoria passo 04 FEITA** (`876eb27`, parecer PRONTO, `reauditorias: 1`, Evidence SHA da spec `99a0747`, nenhum material do incremento mudou — só a governança veio pelo rebase). `pre-implement` → `OK`. **PASSO 06 COMPLETO — 5 tasks:** `0a60d4c` task_01 BILL (§1–§7, 24 entidades no `public`), `1e04850` task_02 LGPD (§8–§15), `23ea1f2` task_03 NOT+API (§16–§22), `46135c4` task_04 IMPEX (§23–§28), `b0903ef` task_05 BI+MOB+FRONTEIRAS (§29–§35). **`docs/arquitetura/modelo-dados/09-plataforma.md` COMPLETO — §1–§35, ~90 entidades** (24 `bill_*` no `public` por `conta_id`, 16 `lgpd_*`, 1 `plat_*` no `public`, 9 `not_*`, 11 `api_*`, 14 `impex_*` — 1 no `public`, 9 `bi_*` — `bi_modelo_exposto` metadado de produto, 6 `mob_*`). Fronteira `public` = **26 entidades** (24 BILL + `plat_catalogo_eventos` + `impex_modelos_planilha`); tudo o mais em `tenant_*`. `not_broadcasts` e `api_versoes_endpoint` movidos p/ `tenant_*` na redação (evita recorrência do REVIEW-001 P2 do 05). Regra genérica de catálogo normativo (5 critérios, §31.1) fecha QA-1 do PRD-02 e QA-5 do PRD-05. SCN-001..010 com subseção de verificação; §35.1 assere `empresa_id`/`conta_id` por entidade (fecha AUD-003). **Passo 07 review APROVADO** (`92c9fda`, Evidence SHA `b0903ef`, 0 P0/P1/P2). 3 P3 aceitos como dívida: REVIEW-001 (§7.4 diz "as 20 entidades de BILL"; correto 24, em §7.1/§31.2), REVIEW-002 (§35.1 "nenhuma entidade de BILL sem `conta_id`" — os 5 catálogos comerciais de §2 não têm), REVIEW-003 (`bi_modelo_exposto` classificado como "metadado de produto" = 3º rótulo de fronteira, ausente dos materiais congelados — **decisão do dono no passo 13**: blessar / tratar como catálogo `public` (→27) / registrar como desvio). **Passo 08 QA APROVADO** (`affa89b`, agente `cz-qa`, Evidence SHA `b0903ef` = mesmo do review): 10/10 SCN PASSOU + RNF, 0 bug, 0 P0/P1/P2; fronteira `public` = 26 consistente entre `09-plataforma.md`/`feature`/`_techspec.md`/`impacto-contratual/dados`. `pre-validate` OK → `sdd-fluxo --run` (`fb45f84`): **`status: validado` / `fase: pr`** → depois `fase: merge`; `metricas.data_validado` marcado. **Passo 10 FEITO — PR #188 ABERTA** (`sdd/09-modelo-dados-plataforma-pr` → main; https://github.com/systeme-dev-br/systeme-erp-docs/pull/188). Base `main` `dc60db3`; `pr/pr-package.md`+`pr/pr-body.md` em `233ee86`; registro em `4b0c922` (pr e plan branch em sync em `4b0c922`). `pr.numero: 188` / `pr.status: aberto` / `pr.head_sha: 233ee86`; `gates.pr.gate_humano: aprovado` (coberto pela aprovação do gate de especificação sob `autonomo_ate_pr`); `gates.merge.gate_humano: pendente`. **PR #188 MERGED** (`a01329c`). **PR #189 consolidação MERGED** (`42a2482` em `main`). **Passo 14 FEITO — PR #190 ABERTA** (branch `sdd/09-modelo-dados-plataforma-aprendizados`, commit `0e691e9`). `sdd/aprendizados/2026-09-10-09-modelo-dados-plataforma.md` (4º ciclo limpo de conteúdo; lacuna do authority checker → #187; `autonomo_ate_pr` validado; retrospectiva da série). `sdd/metricas.csv` append da linha 09 (série 00–09 completa). Patches: **prompt 00** (subir autonomia acima de `assistido` → verificar que o authority checker enumera o gate `especificacao`; pré-requisitos de governança/infra em `main` ANTES de fixar `base_sha`, senão o rebase custa reauditoria) + **prompt 06** (reconciliar contagens/universais com todas as subseções de verificação de SCN + rastreabilidade final — REVIEW-001 do 05, REVIEW-001/002 do 09). `docs/arquitetura/modelo-dados/09-plataforma.md`: REVIEW-001 (§7.4 "20"→24) e REVIEW-002 (§35.1/§31.2 — `conta_id` refinado p/ os 5 catálogos comerciais) corrigidos. `guard` vermelho esperado. **AGUARDANDO MERGE MANUAL DA #190 — que FECHA a série F0-06 inteira (00–09).** Depois: apagar branches `sdd/09-modelo-dados-plataforma-{plan,pr,fechamento,aprendizados}` + `sdd/authority-checker-especificacao` (local+remoto). **PARTE HISTÓRICA (passos 11/13 do 09) abaixo:** PR #189 consolidação foi `235f89d` merge-report + `8309893` consolidação na branch `sdd/09-modelo-dados-plataforma-fechamento`. `pre-consolidate` emitiu atestado (`dados`+`modelo-dados-plataforma`). Criado **`sdd/contratos/modelo-dados-plataforma/contrato.md`** (NOVO — 9 comportamentos). **`sdd/contratos/dados/contrato.md`** ALTERADO — fronteira estendida: 24 entidades de billing no `public` por `conta_id` (exceção estrutural — cobrança da Système) + 2 catálogos normativos da plataforma no `public` sob os 5 critérios; LGPD/NOT/API/IMPEX/BI/MOB em `tenant_*`. **REVIEW-003 decidido pelo dono: `bi_modelo_exposto` = desvio registrado** (config de produto versionada junto do código, nem `tenant_*` nem catálogo `public`; contagem `public` fica em 26). Incremento arquivado em `sdd/historico/2026-09-05-09-modelo-dados-plataforma/{incremento,compozy-tasks}/` + `relatorio-fechamento.md`. Métricas: espec 96h, impl 1h, validado→merge 0h, lead 123h, **reauditorias 1** (rebase pós-#187, não defeito de spec), revisões de plano 0, issues de review 0 (os 3 P3 não foram logados no yaml), bugs 0, P0/P1 0, aderência `fora_do_plano=14/20`. `status: consolidado` / `fase: fechamento`. `sdd/metricas.csv` NÃO na PR #189 (revertido — vai na PR do passo 14). Gate humano PR+merge marcados `aprovado` no yaml. **Próximo: dono mescla PR #189 manualmente (`guard` vermelho esperado); depois passo 14 — aprendizados** (`sdd/aprendizados/2026-09-10-09-modelo-dados-plataforma.md` + append da linha `09-modelo-dados-plataforma,branch,96,1,0,123,1,0,0,0,0,fora_do_plano=14/20` em `sdd/metricas.csv`; alinhar REVIEW-001 [§7.4 "20"→24] e REVIEW-002 [§35.1 "nenhuma entidade de BILL sem conta_id"] em `09-plataforma.md`, que está em `main` desde a #188). **O passo 14 do 09 FECHA a série F0-06 inteira (00–09).** Branches a apagar quando tudo mesclar: `sdd/09-modelo-dados-plataforma-{plan,pr,fechamento,aprendizados}` (local+remoto) + `sdd/authority-checker-especificacao`. **Nota de processo:** o `pre-implement` de cada task deve rodar ANTES de marcar a task como `completed` (task_02 rodou depois → "status completed invalido para start"; `pre-complete` valida o que importa e passou). Editar `task_NN.md` só flipa checkbox + frontmatter `status` + `## Evidências produzidas` (tudo neutralizado pelo normalizador); acrescentar prosa às linhas de subtask quebra o snapshot material.

**Estado histórico anterior (00–07):**
- **00-fundacao:** **CONSOLIDADO** (#161/#164). Contratos `sdd/contratos/modelo-dados-fundacao/` + `sdd/contratos/dados/`.
- **01-multiempresa:** **CONSOLIDADO** (#165/#166 — 1º PR da série verde no CI). Contrato `modelo-dados-multiempresa/` (NOVO) + `dados/` (ALTERADO). Entregável `docs/arquitetura/modelo-dados/01-multiempresa.md`.
- **02-cadastros:** **CONSOLIDADO** (#167 desbloqueio + #168 merge; `bfb7d73` consolida; base `main` fica em `c783a38`). Contrato `modelo-dados-cadastros/`.
- **03-suprimentos: FECHADO — ciclo SDD 14/14 completo.** 3 PRs mesclados: #158 incremento (`73e3a62`), #169 consolidação (`9b844c7`), #170 aprendizados (`fa9f5bf`). `main` tem `sdd/contratos/modelo-dados-suprimentos/` (NOVO) + `dados/` (ALTERADO — cenário suprimentos na fronteira), `sdd/aprendizados/2026-09-08-03-modelo-dados-suprimentos.md`, prompts 07/08/09/11/13 + `_comum.md` com as notas de armadilha (bloco `gates.review`, material pós-auditoria, Evidence SHA hex puro, campos do merge-report, `metricas.csv` fora do PR de consolidação). Incremento arquivado em `sdd/historico/2026-09-05-03-modelo-dados-suprimentos/`. Modelo canônico: `docs/arquitetura/modelo-dados/03-suprimentos.md` §1-§24, ~41 entidades EST/CMP. **Estado abaixo é histórico da execução, arquivado.**
- **03-suprimentos (execução, arquivada, resumo):** 3 rodadas de review+QA (as P3 da rodada 1 corrigidas → invalidaram review/QA; rodada 3 sobre Evidence SHA `19aa316` APROVADO). CI `guard`/`pre-merge-ci` falhou 5x — 4 achados reais viraram armadilhas abaixo; a 5ª (`gate humano 'merge' nao esta aprovado`) é terminal e esperada (merge manual, igual à PR #167 do 02). Decisões do dono AUD-008 (QA-4/7/8/9/10) em `execucao.md` + `PRM-012`. `is_delivery_evidence_path` (mudam sem invalidar review/QA): `auditoria-especificacao.md`, `bugs.md`, `reviews-*`, `qa/*`, `pr/*`, `ops/*`, `incremento.yaml`, `relatorio-fechamento.md`, `sdd/metricas.csv`.
- **04-vendas: FECHADO — ciclo SDD 14/14 completo.** 3 PRs mesclados: **#171** entrega do modelo (`sdd/04-modelo-dados-vendas-pr`, merge `9126694`), **#172** consolidação (`sdd/04-modelo-dados-vendas-fechamento`, merge `f6fc0e4` — `main` fica aqui), **#173** aprendizados (`sdd/04-modelo-dados-vendas-aprendizados`, ABERTA 2026-09-08, aguarda merge manual do dono — `guard` vermelho esperado). `main` tem `sdd/contratos/modelo-dados-vendas/` (NOVO) + `dados/` (ALTERADO), `docs/arquitetura/modelo-dados/04-vendas.md` (§1-§23, ~28 entidades `venda_*`), `sdd/aprendizados/2026-09-08-04-modelo-dados-vendas.md`, e (na #173) patch dos prompts 14/`_comum.md` + backfill de `sdd/metricas.csv` (01/03/04). Incremento arquivado em `sdd/historico/2026-09-05-04-modelo-dados-vendas/` (`fase: aprendizado`). **Ciclo limpo:** 0 commit de correção de formato/CI (contraste com os ~10 do 03) — os patches de prompt do 03 funcionaram. 1 bug real: **REVIEW-001 (P2)** `CHECK` de `venda_pedido_itens` somava `quantidade_devolvida` a `quantidade_pedida` (dupla contagem) → corrigido `d745377`, reauditado/re-revisado/re-QA no Evidence SHA `d7453774`. REVIEW-002/003 + AUD-005 aceitos pelo dono como dívida P3. PR #171 com `guard` FAILURE (gate humano merge), merge manual. Métricas: espec 72h, impl 1h, reauditorias 2, revisões de plano 1, issues 1, 0 bug/P0-P1, aderência 17/18. **Histórico da execução do 04 abaixo (arquivado).**
- **04-vendas (execução, arquivada, resumo):** `status: proposto` / `fase: especificacao` na branch `sdd/04-modelo-dados-vendas-plan` (commit `059e498`, sobre `main` `fa9f5bf`). **Preparação feita:** `base_sha` → `fa9f5bf`; `contratos_lidos` = 4 contratos vivos `modelo-dados-*` + `dados` + docs de modelo 00–03; PRM-010 (deps consolidadas) + PRM-011 (RNF-005); `_prd.md` "Relação com o incremento" reconciliado (lição AUD-009 do 03). **Artefatos existentes:** `brief.md`, `incremento.yaml`, `_prd.md` (draft de 769 linhas de 2026-09-05). **Passo 02 feito** (commit `0804203`): `_techspec.md` + `adrs/adr-001..003` (ADR-001 faturamento evento gerador único; ADR-002 devolução entidade própria; ADR-003 kit composição explodida congelada no pedido). `execucao.md` criado com "Decisões do dono (2026-09-08)". **Decisões do dono QA-3 a QA-10** registradas (execucao.md + PRM-012); QA-1/QA-2 fechadas na TechSpec. `_prd.md` "Perguntas abertas" atualizado. Resumo das decisões: crédito de devolução sem expiração por padrão; orçamento ≠ proposta CRM; "crédito utilizado" = exposição total do cliente; alçada de desconto por categoria suportada; gatilho de comissão parâmetro por empresa (padrão faturamento); expedição **por faturamento**; vencimento pela autorização da NF-e; recorrência sem reembolso proporcional por padrão. **Passo 03 feito** (commit `33fe299`): `INDEX.md` + `task_01..04` + `feature/001__vendas-e-faturamento.feature` (FEAT-001..004, SCN/TST-001..008) + `impacto-contratual/{modelo-dados-vendas (NOVO),dados (ALTERADO)}/contrato.md`. `compozy tasks validate` → `all tasks valid (4 scanned)`; `compozy sync` OK. `fase: especificacao → planejamento`. Grafo: task_01 orçamento/pedido/aprovações/reserva (RF-001–004) → task_02 faturamento/fronteiras VND→NFE/VND→CPR (RF-005/010/011, ADR-001) → task_03 expedição/cancelamento/comissão/devolução (RF-006–009, ADR-002) → task_04 recorrência/consignação/portal/fronteiras/rastreabilidade (RF-012–018). Entregável: `docs/arquitetura/modelo-dados/04-vendas.md`. **1ª auditoria PRONTO** (commit `78b1e3f`, Evidence SHA `33fe299`, sem P0/P1). `status: especificado` / `fase: auditoria`. 4 achados: **AUD-001 (P2)** colisão de IDs PRM entre `_prd.md` (PRM-010/011 = "05/06 aceitarão o faturamento") e `incremento.yaml` (PRM-010 deps consolidadas / PRM-011 RNF-005 / PRM-012 decisões do dono); PRD checklist diz "PRM-001 a PRM-011". **AUD-002 (P2)** `_prd.md` "Impacto no contrato vivo" + checklist dizem `dados: NOVO`, mas impacto-contratual/techspec/INDEX/execucao/incremento.yaml dizem `dados: ALTERADO` (correto — `sdd/contratos/dados/` existe). **AUD-003 (P3)** ancoragem de BR-002/003/004 no INDEX. **AUD-004 (P3, aceito)** dispensa do gate humano de especificação. Corrigidos AUD-001/002/003 (`0a7aedf`). **Reauditoria 2 PRONTO** (commit `45ebf94`, Evidence SHA `0a7aedf`, sem P0/P1). AUD-004 aceito; **AUD-005 (P3, novo, aceito)** — `execucao.md` §"Aprovação do plano" ainda diz `Status: PENDENTE` (cosmético; gate canônico é `gates.especificacao` no yaml; dívida documental p/ próxima revisão de plano — igual ao AUD-011 do 03). **Reauditoria 2 PRONTO** (`45ebf94`, Evidence SHA `0a7aedf`, sem P0/P1). **As 4 tasks CONCLUÍDAS** (`f0d9898`/`3798545`/`9d509a1`/`a353fb0`). `docs/arquitetura/modelo-dados/04-vendas.md` **§1-§23** (~30 entidades VND): orçamento/pedido/aprovações/reserva; faturamento N por pedido + fronteiras VND→NFE/VND→CPR; expedição por faturamento/cancelamento/comissão+split/devolução entidade própria; recorrência idempotente/consignação/portal/`venda_parametros`; §17 mapa `public`×`tenant_*` (SCN-007 §17.1), §18 trilha auditoria, §19 desvios F0-05, §20 fora de escopo (SCN-008 §20.1), §21 ADRs, §22 deps 05/06/08, §23 rastreabilidade final. SCN-001..008 todos com subseção verificável. Gates verdes em cada task. **`status: em_execucao` / `fase: review`.** **`cz-revisor-implementacao` EM ANDAMENTO** sobre Evidence SHA `a353fb0` (base do diff = `fa9f5bf`/`origin/main`; ref local `main` está velho em `c783a38` — não usar).
- **05-fiscal: FECHADO — ciclo SDD 14/14.** 3 PRs: **#174** modelo (`sdd/05-modelo-dados-fiscal-pr`, merge `5761071`), **#175** consolidação (`sdd/05-modelo-dados-fiscal-fechamento`, merge `668bd9f` — `main` fica aqui), **#176** aprendizados (`sdd/05-modelo-dados-fiscal-aprendizados`, ABERTA — aguarda merge manual do dono; `guard` vermelho esperado). `main` tem `docs/arquitetura/modelo-dados/05-fiscal.md` (§1–§19, ~44 entidades), `sdd/contratos/modelo-dados-fiscal/` (NOVO) + `dados/` (ALTERADO), `sdd/aprendizados/2026-09-09-05-modelo-dados-fiscal.md` (na #176) + patches dos prompts 04/06/07/14/`_comum.md` + backfill `sdd/metricas.csv` 00–05 (na #176). Incremento arquivado em `sdd/historico/2026-09-05-05-modelo-dados-fiscal/` (`fase: aprendizado`). **Ciclo:** reauditoria 1 (dependencies entre aspas → `sdd-guard pre-implement` rejeita), REVIEW-001 P2 (6ª linha `public` em §16 vs "5 catálogos"/impacto-contratual) + 2 P3, 2 rodadas de review, 0 bug de QA, 0 P0/P1. Métricas: espec 99h, impl 8h, reauditorias 1, revisões de plano 0, issues de review 3, aderência 15/21, lead 108h. **Aprendizados promovidos:** auditoria roda `sdd-guard pre-implement task_01` (prompt 04); conferir contagem de linhas `public` da fronteira vs número declarado (prompts 06/07); PR do passo 14 commita `sdd/metricas.csv` completo desde o último ponto versionado (prompt 14/`_comum.md`). A #173 (04) mesclou (`17fdd47`) enquanto a #176 estava aberta → #176 conflitou (`sdd/metricas.csv`, `sdd/prompts/14`) e foi **rebaseada** sobre `main` já com #173 (commit `86f3e2d`, force-push): agora só acrescenta a linha do 05 ao CSV (00–04 vieram de #173) e a cláusula nova ao prompt 14; `_comum.md` saiu (idêntico ao de #173). #176 **MESCLADA** (`9d0c2e1`, `main` fica aqui). Lição registrada: PR de passo 14 pendente deixa `sdd/metricas.csv` incompleto em `main` e faz a próxima nascer defasada. **PR #160 FECHADA** (2026-09-09, sem merge) — era rascunho abandonado do planejamento inicial do 04 (branch `sdd/04-modelo-dados-vendas-plan`, 119 commits atrás, TechSpec/ADRs superados); branch remota apagada. **Zero PRs abertas.** **Próximo: 06-modelo-dados-financeiro** (`proposto`, nunca auditado). **Histórico da execução do 05 abaixo.**
- **05-fiscal (execução, arquivada, resumo): EM ESPECIFICAÇÃO** na branch `sdd/05-modelo-dados-fiscal-plan` (a partir de `origin/main` `f6fc0e4`; não empurrada). `rigor: large`, `risco: medio`, `autonomia: assistido`, `alvo: branch`. Domínios `modelo-dados-fiscal` (NOVO) + `dados` (ALTERADO). 6 módulos coesos: EVT → NFE → NFCE → NFSE → CTE → SPED (EVT precede todos). Commits: **`dd8963b`** prepara base (base_sha `4f6746a`→`f6fc0e4`, contratos_lidos 00–04, PRM-010 deps/PRM-011 RNF-005); **`b83c085`** passo 01 — revisão do PRD de 966 linhas (2026-09-05): corrigiu de saída os 2 achados do ciclo 04 — `dados: NOVO`→`ALTERADO` e colisão PRM-010/011 PRD×yaml (premissas prospectivas do PRD → PRM-013/014/015, nova PRM-012 "sem revisão de contador"); **`a8555c0`** passo 02 — `_techspec.md` (381 linhas) + `adrs/adr-001..005` (001 barramento de evento único; 002 contingência FSM genérica por empresa×tipo; 003 numeração fonte no emissor + painel projetado; 004 SPED entidades de processo; 005 repositório central imutável); **`9ba34a8`** passo 03 — `INDEX.md` + `task_01..05` + `feature/001__fiscal-eventos-e-emissores.feature` (FEAT-001..005, SCN/TST-001..010) + `impacto-contratual/{modelo-dados-fiscal (NOVO),dados (ALTERADO)}` + `execucao.md` (base `f6fc0e4`, RSK-001..005, gate humano do plano dispensado). `compozy tasks validate` → `all tasks valid (5 scanned)`; `compozy sync` OK. `fase: especificacao → planejamento`. Grafo: task_01 EVT núcleo (RF-001–003) → task_02 EVT capacidades/repositório/numeração/trilha (RF-004–006) → task_03 NFE+NFCE (RF-007–009) → task_04 NFSE+CTE (RF-010–011) → task_05 SPED+fronteiras+desvios+rastreabilidade (RF-012–016). Entregável: `docs/arquitetura/modelo-dados/05-fiscal.md`. Fronteiras VND→NFE (04) e SPED→CPR (06). **Auditoria PRONTO** (commit `b87b06c`, Evidence SHA `9ba34a8`, agente `cz-auditor-especificacao` em worktree isolado, sem P0/P1). `status: proposto → especificado` / `fase: planejamento → auditoria`; `compozy tasks validate` verde (5 tasks); `sdd/contratos/` intocado. Achados: **AUD-001/002 (P2)** = os mesmos do ciclo 04 (dados NOVO→ALTERADO; colisão PRM) — já pré-corrigidos no passo 01 e verificados. **AUD-006** (corte de 5 tasks) e **AUD-007** (dispensa gate humano com dados ALTERADO) aceitos pelo auditor. **AUD-003/004/005 (P3)** aceitos pelo dono como dívida documental (`cd1f094`): AUD-004 (nomear desvio `nfe_emitidas.pedido_id`→faturamento do 04 na seção RF-015) e AUD-005 (motivo por catálogo `public`, não rótulo comum) → sanar ao redigir `05-fiscal.md`; AUD-003 (typo no cabeçalho do `_prd.md`, artefato material) → revisão de plano futura. **Reauditoria PRONTO** (`3cdc62e`, Evidence SHA **`5fc479f`**, `metricas.reauditorias: 1`): `task_02..05` tinham `dependencies: ["task_NN"]` entre aspas — `compozy tasks validate` aceita mas `sdd-guard pre-implement/pre-merge-ci` exige `[task_NN]` (regex `task_[0-9]{2}`, como no 04). Corrigido em `5fc479f`; **AUD-008 (P3, resolvido)**. **Implementação em curso (passo 06):** `docs/arquitetura/modelo-dados/05-fiscal.md`. **task_01 CONCLUÍDA** (`5ce54c9`): §1 escopo/regras transversais (BR-003/004/005/006/009/010/015); §2 `fiscal_evento_envelope` (barramento único, `UNIQUE (tipo_documento, documento_id, sequencia)`, `idempotency_key`, `ambiente` isolado — ADR-001) + `fiscal_evento_erros` + vocabulários; §3 `fiscal_autoridade_status`/`fiscal_rejeicao_dicionario` (`public`) + `_complementos` (tenant); §4 contingência (modos `public`, estado FSM empresa×tipo, config, pendentes, `retransmissao_fila`) — ADR-002. SCN-001 §2.3, SCN-002 §4.6. **task_02 CONCLUÍDA** (`ded30e1`): §5 assinatura (só `certificado_id` do cofre do 01)/representação determinística/impressão por caixa/entrega multicanal; §6 `fiscal_documentos` (repositório imutável, `hash_integridade`, `retido_ate` ≥ 5 anos precede LGPD, fonte única, emissores só referenciam `fiscal_documento_id` — ADR-005) + `fiscal_documento_acessos`; §7 painel de numeração projetado sem estado próprio (ADR-003); §8 `fiscal_trilha` (view somente leitura). SCN-003 §6.3, SCN-004 §7.2. **task_03 CONCLUÍDA** (`dbc750d`): §9 `nfe_configuracoes` (série/número por empresa/filial/ambiente, `certificado_id` do cofre do 01) + `nfe_documentos` (nasce de `venda_faturamento_id` do 04, `UNIQUE` quando `natureza=venda` — BR-008; status BR-016; rejeição não consome / denegação consome — BR-007) + `nfe_documento_itens` (regra fiscal aplicada + versão, override auditado); §10 `nfe_eventos` (canc/CC-e/inutilização) + `nfe_numeracao_inutilizacoes` + `nfe_regras_validacao` + devolução/complementar/exportação; §11 `nfce_configuracoes` (numeração por caixa) + `nfce_documentos` (`venda_pdv_id` chave futura, QR do CSC, contingência `offline_local`) + `nfce_eventos`. SCN-005 §9.4, SCN-006 §11.3. **task_04 CONCLUÍDA** (`01b03de`): §12 `nfse_municipios_catalogo` (`public`) + `nfse_configuracoes` + `nfse_rps`/`nfse_documentos` (`municipio_iss_devido` pelo local da prestação, `iss_retido`, retenções federais) + `nfse_eventos` (canc/subst, nunca edição — BR-011); §13 `cte_configuracoes` (série por modelo 57/67/58) + `cte_veiculos`/`cte_condutores` + `cte_documentos` (`tomador` distinto das partes, `carga`=ref NF-e, `override` justificado) + `cte_eventos` + `cte_mdfe` com índice único parcial `(empresa_id, veiculo_id) WHERE status='Autorizado'` (BR-012). SCN-007 §12.5, SCN-008 §13.4. **task_05 CONCLUÍDA** (`420ebb9` — última): §14 `sped_dfe_distribuicao`/`sped_manifestacoes` + `sped_escrituracoes` (`conjunto_origem` obrigatório, retificadora — BR-013, ADR-004); §15 `sped_apuracoes`/`sped_guias` (saldo→guia + `titulo_evento_ref` p/ o 06 — fronteira SPED→CPR) + `sped_obrigacoes_catalogo` (`public`)/`sped_calendario`/`sped_checklist_mensal`; **§16 fronteira `public`×`tenant_*` por entidade com motivo por catálogo (fix AUD-005)**; **§17 desvios da F0-05 incluindo `nfe_emitidas.pedido_id`→`venda_faturamento_id` do 04 (fix AUD-004)**; §18 fora de escopo; §19 rastreabilidade + SCN-010 §19.1. SCN-009 §15.3. **`docs/arquitetura/modelo-dados/05-fiscal.md` COMPLETO** (589 linhas, §1–§19, ~35 entidades `fiscal_*`/`nfe_*`/`nfce_*`/`nfse_*`/`cte_*`/`sped_*`). Todas as 5 tasks `completed`; gates verdes. **`status: em_execucao` / `fase: review`.** **Review rodada 1** (`226c500`, Evidence SHA `420ebb9`): `- Status:` normativo **APROVADO** (sem P0/P1), operacional "APROVADO COM RESSALVAS". **REVIEW-001 (P2)**: §2.2/§16 punham os vocabulários `tipo_documento`/`tipo_evento` como 6ª estrutura `public`, contra "apenas 5 catálogos" (§16-intro/§19.1/feature SCN-010/impacto-contratual `dados`). **REVIEW-002/003 (P3)**: §1 lista de catálogos `public` stale; §7-intro chave de numeração frouxa vs §9.1. **Correções** (`d9cfeab`, novo Evidence SHA; `bugfix-report.md`): vocabulários viram listas de valores fixas do doc (`CHECK`/enum, sem schema), §16 volta a 5 catálogos `public`; §1/BR-015 apontam §16; §7-intro alinhado. Só `05-fiscal.md` (não material) + evidência de entrega tocados; **sem re-audit** (nenhum material). `compozy tasks validate` verde. **Re-review rodada 2 APROVADO** (`2b2ca05`, Evidence SHA **`d9cfeab`**, sem P0/P1/P2/P3 novo; REVIEW-001/002/003 resolvidos). Obs residual não bloqueante: §4.6/§11.3 usam tuplas abreviadas na prosa de verificação (classe do REVIEW-003, sem regressão) — retoque opcional na consolidação (passo 13). `fase: review → qa` (`6dc4e0e`). **QA APROVADO** (`fe35267`, agente `cz-qa`, Evidence SHA `d9cfeab` = mesmo do review): 5 tasks / 10 SCN PASSOU, 0 bug, `scan-secrets` OK, `compozy tasks validate` verde. `pre-validate` OK → `sdd-fluxo.sh --run` aplicou **`status: validado` / `fase: pr`** (`aaefb51`); `metricas.data_validado` marcado. Métricas até aqui: reauditorias 1, revisões de plano 0, issues de review 3 (REVIEW-001 P2 + 2 P3, todas resolvidas), bugs 0, P0/P1 0. **PR #174 ABERTA** (`docs(sdd): modelo de dados fiscal (05)…`, branch `sdd/05-modelo-dados-fiscal-pr` a partir da branch de plano em `aaefb51`; commits `900b351` pacote / `7c2d178` registra PR / `d697bb9` fase merge; empurrada). Estado GitHub: OPEN / MERGEABLE; check `guard` vermelho esperado por `require_human_gate merge`. `pr/pr-package.md` + `pr/pr-body.md` escritos; `pre-validate`/`scan-secrets` verdes; `sdd/contratos/` intocado. `incremento.yaml` `pr.numero: 174` / `status: aberto`; **`status: validado` / `fase: merge`**. **PR #174 MERGED** pelo dono (merge commit `5761071` em `main`; check `guard` FAILURE só pelo gate humano). `pr/merge-report.md` registrado (Status MERGED, Merge SHA `5761071`, Head SHA validado = Evidence SHA `d9cfeab`). `incremento.yaml` `pr.status: merged` / `pr.head_sha: 5761071`; `metricas.data_merge` marcado. Branch **`sdd/05-modelo-dados-fiscal-fechamento`** criada de `origin/main`, commit `2a7a0ac` (registra merge) — não empurrada. `status: validado` / `fase: merge`. **Consolidação FEITA** (passo 13, commit `063aa5a` na branch `sdd/05-modelo-dados-fiscal-fechamento`, empurrada; **PR #175 ABERTA**). `pre-consolidate` emitiu atestado (domínios `dados`+`modelo-dados-fiscal`) antes das escritas. Criado **`sdd/contratos/modelo-dados-fiscal/contrato.md`** (NOVO — 6 comportamentos: modelo por tenant, barramento único, repositório imutável, VND→NFE + SPED→CPR, contingência/numeração declaradas uma vez). **`sdd/contratos/dados/contrato.md`** ALTERADO — cenário "Dados fiscais permanecem no tenant, com cinco catálogos globais" + lista de catálogos `public` estendida. Incremento arquivado em `sdd/historico/2026-09-05-05-modelo-dados-fiscal/{incremento,compozy-tasks}/` + `relatorio-fechamento.md` (métricas: espec 99h, impl 8h, reauditorias 1, revisões de plano 0, issues de review 3, 0 bug/P0-P1, aderência 15/21, lead 108h). `status: consolidado` / `fase: fechamento`. `sdd/metricas.csv` NÃO entrou na PR (revertido localmente — vai na PR do passo 14). Gate humano PR+merge marcados `aprovado` no yaml. **Próximo: dono mescla PR #175 manualmente (`guard` vermelho esperado: mudança de produto `sdd/*` sem incremento ativo + gate humano); depois passo 14 — promover aprendizados** (`sdd/aprendizados/2026-09-09-05-modelo-dados-fiscal.md` + backfill de `sdd/metricas.csv` com 05, ver lição do passo 14 do 04). **05-fiscal.md em `main`** desde a PR #174 (merge `5761071`). **Nota de dívida:** `execucao.md` §"Aprovação do plano" diz `Status: PENDENTE` e §"Revisões" diz reauditoria pendente + obs cosméticas §4.6/§11.3/§19 (tuplas abreviadas na prosa de verificação) — retoque opcional na consolidação (passo 13); nada bloqueia.
- **06-financeiro: FECHADO — ciclo SDD 14/14.** 3 PRs: **#177** modelo (merge `2693601`), **#178** consolidação (merge `bc2f350` — `main` fica aqui), **#179** aprendizados (`sdd/06-modelo-dados-financeiro-aprendizados`, ABERTA — aguarda merge manual; `guard` vermelho esperado). `main` tem `docs/arquitetura/modelo-dados/06-financeiro.md` (§1–§21, ~40 entidades CPR/CNB/DRE), `sdd/contratos/modelo-dados-financeiro/` (NOVO) + `dados/` (ALTERADO — bloco financeiro sem exceção de `public`), `sdd/aprendizados/2026-09-09-06-modelo-dados-financeiro.md` (na #179) + patches dos prompts 04/06 + `sdd/metricas.csv` 00–06 (na #179). Incremento arquivado em `sdd/historico/2026-09-05-06-modelo-dados-financeiro/` (`fase: aprendizado`). **Ciclo mais limpo da série:** 0 reauditoria, 0 revisão de plano, 0 bug, nenhum P0/P1/P2 (só 2 P3 de review aceitos). Os patches do ciclo 05 (`dependencies` canônico `[task_NN]`, `sdd-guard pre-implement task_01` na auditoria, contagem da fronteira `public`) funcionaram. **Aprendizados promovidos:** `empresa_id` explícito nas tabelas de entidade-detalhe (prompt 06); auditoria atualiza `execucao.md` §"Aprovação do plano"/gate de auditoria no commit da auditoria (prompt 04 — elimina o AUD-005 recorrente do 04). Métricas: espec 109h (maior da série), impl 1h (menor), issues de review 2, aderência 13/18, lead 110h. **Histórico da execução do 06 abaixo.**
- **07-pdv: FECHADO — ciclo SDD 14/14.** 3 PRs TODAS MESCLADAS: **#180** modelo (merge `2e0cec6`), **#182** consolidação (merge `301d542`), **#183** aprendizados (merge `44c6e97` — `main` fica aqui). Branches `sdd/07-modelo-dados-pdv-{plan,pr,fechamento,aprendizados}` apagadas. `main` tem `docs/arquitetura/modelo-dados/07-pdv.md` (583 linhas, §1–§21, ~15 entidades `pdv_*` — todas `tenant_*`, contagem `public` = **zero**), `sdd/contratos/modelo-dados-pdv/` (NOVO — 5 comportamentos) + `dados/` (ALTERADO — cenário "Dados de PDV permanecem no tenant"), e na #183: `sdd/aprendizados/2026-09-09-07-modelo-dados-pdv.md` + patches dos prompts 04/07/08/13 + `sdd/metricas.csv` 00–07. Incremento arquivado em `sdd/historico/2026-09-05-07-modelo-dados-pdv/` (`fase: aprendizado`). Base `b6be00f`. Evidence SHA (review+QA) `4e8abe5`. ADR-001 caixa físico × turno separados; ADR-002 fila de sync/idempotência no PDV (MOB é consumidor); ADR-003 numeração de cupom por terminal. 4 tasks VENDA/CAIXA/OFFLINE/FRONTEIRAS; SCN-001..008 com subseção de verificação. **2º ciclo limpo seguido:** 0 reauditoria, 0 revisão de plano, 0 bug, nenhum P0/P1/P2 — só 3 P3 (AUD-001 numeração PRM no checklist; AUD-002 ref cruzada PRM-012/013; REVIEW-001 rótulo "(lógico →)" em 4 FKs intra-bloco; REVIEW-002 `pdv_autorizacoes` entidade nova fora do plano → revisão de plano registrada no `relatorio-fechamento.md`; REVIEW-003 impacto `dados` da task_02). Métricas: espec 96h, impl 1h, reauditorias 0, revisões de plano 0, issues de review 3, 0 bug/P0-P1-P2, aderência 13/17, lead 114h. **Aprendizados promovidos:** prompt 04 **REVERTE o patch do 06** — a auditoria NÃO edita `execucao.md` (é material do snapshot; `## Aprovação do plano` fica `PENDENTE` por construção — o normalizador não neutraliza `- Status:` em prosa; a transição vive só nos campos normalizados do `incremento.yaml`); prompts 07/08 não escrevem os literais `-XXX`/`SCN-xxx (TST-xxx)`/`NAO_VERIFICADO` nos relatórios de evidência, nem em negação (o `sdd-guard` varre por string — custou 1 ida e volta cada); prompt 13 `git add` do `incremento.yaml` antes do `git mv` do diretório na consolidação (senão o commit captura o yaml velho). **Recomendação de autonomia:** manter `assistido` no 08; se 08 fechar limpo (3 seguidos), avaliar `autonomo_ate_pr` só para a mecânica documental — decisão do dono. **Próximo: 08-modelo-dados-crm** (`proposto`, nunca auditado).
- **06-financeiro (execução, arquivada, resumo): EM ESPECIFICAÇÃO** na branch `sdd/06-modelo-dados-financeiro-plan` (de `origin/main` `9d0c2e1`; não empurrada). `rigor: medium`, `risco: medio`, `autonomia: assistido`, `alvo: branch`. Domínios `modelo-dados-financeiro` (NOVO) + `dados` (ALTERADO). 3 módulos: **CPR** (títulos a pagar/receber, baixas imutáveis, borderô, renegociação, régua de cobrança, adiantamentos, rateio), **CNB** (contas bancárias/convênios, remessa/retorno CNAB, extrato OFX, conciliação, tarifas), **DRE** (plano de contas, centros de custo hierárquicos, rateio, fluxo de caixa com cenários, DRE por competência/centro, orçamento, provisão). Consome CMP→CPR (03), VND→CPR (04), SPED→CPR (05). Commits: **`246947e`** prepara base (base_sha `4f6746a`→`9d0c2e1`, contratos_lidos 6 contratos vivos + docs 00–05, PRM-010 deps/PRM-011 RNF-005); **`fd659b5`** passo 01 — revisão do PRD de 1039 linhas: `dados: NOVO`→`ALTERADO` (financeiro é inteiramente `tenant_*`, sem exceção de `public`); colisão PRM (PRD PRM-010/011/012 → realinhadas 010 deps/011 RNF-005/012 sem revisão de contador; prospectivas → PRM-013/014). **Passo 02 feito** (`30aad60`): `_techspec.md` (347 linhas) + `adrs/adr-001..004` (001 título/parcela entidade única; 002 baixa imutável, correção só por estorno com contrapartida; 003 conciliação liga extrato ao lançamento de caixa, nunca ao título; 004 competência governa a DRE, movimento governa o caixa). Saldo do título = coluna mantida + invariante `saldo = valor − SUM(baixas) + SUM(estornos)` (QA-4, prosa da TechSpec, espelha ADR-001 do 03). Bloco inteiramente `tenant_*`. Sequenciamento 4 tasks. **Passo 03 feito** (`88548e9`): `INDEX.md` + `task_01..04` (`dependencies` na forma canônica `[task_NN]` — lição do 05) + `feature/001__financeiro-titulos-conciliacao-dre.feature` (FEAT-001..004, SCN/TST-001..008) + `impacto-contratual/{modelo-dados-financeiro (NOVO),dados (ALTERADO)}` + `execucao.md` (base `9d0c2e1`, RSK-001..006, gate humano do plano dispensado). `depende_de`/PRM-009 passam a listar o 05. `compozy tasks validate` → `all tasks valid (4 scanned)`; `compozy sync` OK. `fase: especificacao → planejamento`. Grafo: task_01 CPR título/parcela/baixa (RF-001–003) → task_02 CPR cobrança/retorno/acordos (RF-004–007) → task_03 CNB extrato/lançamento/conciliação (RF-008–012) → task_04 DRE+fronteiras+desvios+rastreabilidade (RF-013–020). Entregável: `docs/arquitetura/modelo-dados/06-financeiro.md`. **Auditoria PRONTO** (`bb8b7e5`, Evidence SHA `88548e9`, agente em worktree isolado, sem P0/P1; `pre-implement task_01` passa). `status: proposto → especificado` / `fase: planejamento → auditoria`; `compozy tasks validate` verde; `sdd/contratos/` intocado; fronteira `public`×`tenant_*` = **zero exceção** consistente (o ponto do REVIEW-001 P2 do 05, aqui limpo). **4 achados P3 aceitos pelo dono como dívida** (`f7ba7ea`): AUD-001 (`_prd.md` diz "PRM-012 alinhada ao yaml", mas é premissa do README), AUD-002 (decisão do saldo do título em prosa da TechSpec, foi ADR-001 no 03 — **fica em prosa, sem adr-005**), AUD-003 (legenda `Regra:` do SCN-008 no `.feature` cita mal a BR-001), AUD-004 (RF-018/RF-019 sem SCN dedicado — ancorar em subseções na redação da task_04). **Implementação em curso (passo 06):** `06-financeiro.md`. **task_01 CONCLUÍDA** (`9a56157`): §1 escopo/regras transversais; §2 `financeiro_titulos` (entidade única título/parcela — ADR-001; `saldo` coluna + invariante; `data_competencia`) + `financeiro_titulo_referencias`; §3 `financeiro_condicoes_pagamento` + parcelamento; §4 `financeiro_baixas` só-inserção + `_estornos`/`_baixa_encargos`/`_regras_encargos`/`_aprovacoes` (ADR-002). SCN-001 §2.2, SCN-002 §4.4. **task_02 CONCLUÍDA** (`788b5ae`): §5 `financeiro_borderos`/`_bordero_titulos` (`UNIQUE` parcial "um título não em dois borderôs ativos", baixa só na confirmação); §6 `financeiro_boletos` (`nosso_numero` da faixa, `UNIQUE` parcial "um boleto ativo por título") + réguas; §7 `financeiro_retornos` (`UNIQUE (empresa_id, hash_arquivo)` — BR-012) + `_retorno_ocorrencias` (casa pelo `nosso_numero`); §8 `financeiro_renegociacoes` (baixa `Renegociado` sem caixa, cancelamento restaura) + `_rateios`/`_rateio_modelos` (`SUM = 100%`) + `_adiantamentos`/`_adiantamento_compensacoes` (FIFO). SCN-003 §7.2, SCN-004 §8.4. **task_03 CONCLUÍDA** (`ac87200`): §9 `financeiro_contas_bancarias` + `financeiro_convenios_cobranca`/`_pagamento` (`credencial_ref` → cofre do 09); §10 `financeiro_remessas` (`UNIQUE (empresa_id, hash_arquivo)`; pagamento nasce de borderô) + retorno de pagamento reusa `financeiro_retornos`; §11 `financeiro_extratos` + `financeiro_extrato_lancamentos` (`UNIQUE (conta_bancaria_id, fitid)` — BR-010) + `financeiro_lancamentos_caixa` (linha por baixa + por encargo, estorno por contrapartida); §12 `financeiro_conciliacoes` **N:1 extrato↔lançamento, sem coluna `titulo_id`** (BR-011, ADR-003) + `financeiro_regras_conciliacao` + `financeiro_fechamentos_conciliacao` por conta×competência (BR-015). SCN-005 §12.4, SCN-006 §12.5. **task_04 CONCLUÍDA** (`9dc22d9` — última): §13 `financeiro_plano_contas` (3 níveis) + `financeiro_categorias` (vínculo obrigatório — BR-014) + `_plano_depara`; §14 `financeiro_centros_custo` (hierárquico) + `_direcionadores`; §15 `financeiro_fluxo_cenarios` (somente leitura — BR-016) + `_orcamentos` + `_provisoes_devedores`; DRE por competência lê `data_competencia` + CMV do 03 (ADR-004). SCN-007 §15.3; §16 `financeiro_fechamentos_periodo` (distinto do de conciliação — BR-015); **§17 fronteira `public`×`tenant_*` — TODAS as entidades `tenant_*`, zero exceção de `public`**; §18 `financeiro_trilha`; **§19 desvios F0-05 (resolve AUD-004 — RF-018/019 ancorados em §18/§19)**; §20 fora de escopo; §21 rastreabilidade + SCN-008 §21.1. **`docs/arquitetura/modelo-dados/06-financeiro.md` COMPLETO** (528 linhas, §1–§21, ~42 entidades `financeiro_*`). Todas as 4 tasks `completed`; gates verdes. **`status: em_execucao` / `fase: review`.** **Review rodada 1 APROVADO** (`a84e116`, Evidence SHA `9dc22d9`, sem P0/P1/P2). 2 P3 aceitos pelo dono (`ea05950`): **REVIEW-001** (§1/§21.1 dizem "nenhuma entidade sem `empresa_id`", mas ~16 tabelas de detalhe listam só a FK do pai — imprecisão de redação, sem falha de tenancy; convenção do 03/05 repete `empresa_id`), **REVIEW-002** (checklist de gates do `execucao.md` desatualizado — artefato material). `fase: review → qa`. **QA APROVADO** (`d3f8628`, agente `cz-qa`, Evidence SHA `9dc22d9` = mesmo do review): 4 tasks / 8 SCN PASSOU, 0 bug, `scan-secrets` OK (produto + 18 artefatos), `compozy tasks validate` verde. `pre-validate` OK → `sdd-fluxo --run` aplicou **`status: validado` / `fase: pr`** (`a96db8d`); `metricas.data_validado` marcado; issues REVIEW-001/002 `open → closed` (vocabulário do guard: aceite documental fecha o issue). Métricas até aqui: reauditorias 0, revisões de plano 0, issues de review 2 (P3, closed), bugs 0, P0/P1 0. **Nota de dívida:** `execucao.md` gates/§"Aprovação do plano" desatualizados (REVIEW-002 + classe do AUD-005 do 04) — revisão de plano futura. **PR #177 ABERTA** (`docs(sdd): modelo de dados financeiro (06) — CPR, CNB, DRE`, branch `sdd/06-modelo-dados-financeiro-pr` a partir da de plano em `a96db8d`; commits `2690872` pacote / `ffc7b31` registra PR + fase merge; empurrada). Estado GitHub: OPEN / MERGEABLE; check `guard` vermelho esperado por `require_human_gate merge`. `pr/pr-package.md` + `pr/pr-body.md` escritos; `pre-validate`/`scan-secrets` verdes; `sdd/contratos/` intocado. `incremento.yaml` `pr.numero: 177` / `status: aberto`; **`status: validado` / `fase: merge`**. **PR #177 MERGED** pelo dono (merge commit `2693601` em `main`; check `guard` FAILURE só pelo gate humano). `pr/merge-report.md` registrado (Status MERGED, Merge SHA `2693601`, Head SHA validado = Evidence SHA `9dc22d9`). `incremento.yaml` `pr.status: merged` / `pr.head_sha: 2693601`; `metricas.data_merge` marcado. Branch **`sdd/06-modelo-dados-financeiro-fechamento`** criada de `origin/main`, commit `cf61f5d` (registra merge) — não empurrada. `status: validado` / `fase: merge`. Gate humano PR+merge a marcar `aprovado` no yaml antes do `pre-consolidate` (lição do 05). **Consolidação FEITA** (passo 13, commit `08ef5d6` na branch `sdd/06-modelo-dados-financeiro-fechamento`, empurrada; **PR #178 ABERTA**, OPEN/MERGEABLE, 31 arquivos = só `sdd/contratos/*` + `sdd/historico/*`). `pre-consolidate` emitiu atestado (`dados`+`modelo-dados-financeiro`) após marcar `gates.pr`/`gates.merge` como `aprovado`. Criado **`sdd/contratos/modelo-dados-financeiro/contrato.md`** (NOVO — 4 comportamentos: modelo por tenant, título/parcela entidade única + saldo invariante, baixa imutável + conciliação via lançamento de caixa, competência↔DRE/movimento↔caixa). **`sdd/contratos/dados/contrato.md`** ALTERADO — cenário "Dados financeiros permanecem no tenant, sem exceção de `public`". Incremento arquivado em `sdd/historico/2026-09-05-06-modelo-dados-financeiro/` + `relatorio-fechamento.md` (métricas: espec 109h, impl 1h, reauditorias 0, revisões de plano 0, issues de review 2, 0 bug/P0-P1-P2, aderência 13/18, lead 110h). `status: consolidado` / `fase: fechamento`. `sdd/metricas.csv` NÃO na PR (revertido — vai na PR do passo 14). **Próximo: dono mescla PR #178 manualmente (`guard` vermelho esperado); depois passo 14 — promover aprendizados** (`sdd/aprendizados/2026-09-09-06-modelo-dados-financeiro.md` + append da linha do 06 em `sdd/metricas.csv`; base ≥ merge de #178). **06-financeiro.md em `main`** desde a PR #177 (merge `2693601`).

**Ciclo por incremento:** destravar (`status: bloqueado→proposto`, `base_sha` ≥ commit da última consolidação, registrar dispensas RNF-005/compozy, atualizar refs de contrato) → `cz-auditor-especificacao` (worktree isolado) → implementar tasks (prompt 06, `pre-implement`/`pre-complete` por task) → `cz-revisor-implementacao` → `cz-qa` → `pre-validate` → `status: validado` → PR (dono mescla manual) → passo 13 consolidação (`pre-consolidate` → `sdd/contratos/<dominio>/contrato.md` + `sdd/historico/AAAA-MM-DD-<feat>/`; PR separado).

**Armadilhas conhecidas:**
- **`sdd-guard.sh` compara o worktree contra o Evidence SHA auditado para os artefatos MATERIAIS (`_prd.md`, `_techspec.md`, `INDEX.md`, `execucao.md`, tasks, `.feature`, ADRs). Qualquer edição neles pós-auditoria — mesmo cosmética P3 — dá "worktree divergiu ... reaudite". Só `auditoria-especificacao.md` e os campos de trilha do `incremento.yaml` (status/fase/gates/pr/deploy/metricas/bloqueio/fechamento) ficam fora do snapshot.** Por isso P3 "aceite" fica como está até a próxima revisão real.
- **NÃO adicionar bloco `gates.review` (nem `gates.qa`) ao `incremento.yaml`.** O normalizador do guard dropa `gates.*` mas PRESERVA `gates.*.gate_humano.requerido`; um bloco `gates.review` novo (que o `cz-revisor-implementacao` tende a inventar) não existe no Evidence SHA auditado → check `guard` do CI (`pre-merge-ci`) falha com "index divergiu do Evidence SHA … incremento.yaml". Os incrementos 00/01/02 mesclados só têm `gates: especificacao/pr/merge/producao`. Parecer do review/QA fica só em `reviews-001/review-report.md` e `qa/*-qa-report.md`. Corrigido no 03 pelo commit `ad57f37`.
- Evidence SHA no `incremento.yaml`/auditoria: hex puro, SEM crases — o guard rejeita com backticks.
- `fase` pós-PRONTO (gate humano dispensado) = `auditoria`, não "especificação" (nem é valor válido); conferir com `sdd-fluxo.sh`.
- `base_sha` do incremento tem de ser ≥ commit da última consolidação (senão `check_contracts_untouched` falha). Corrigir ANTES da auditoria.
- QA report: sem o literal `NAO_VERIFICADO`, sem Evidence SHA entre backticks. Review e QA usam o MESMO Evidence SHA (commit da implementação).
- PR de consolidação: NÃO incluir `sdd/metricas.csv`. Só `sdd/contratos/*` + `sdd/historico/*`.
- **`sdd/metricas.csv` versionado é commitado pela PR do passo 14** (não pelo passo 13). Aprendizado do 04: o passo 13 mantém o arquivo fora da sua PR e nada mais o commitava → série ficou 3 incrementos stale (só 00/02) até a #173 fazer o backfill. Prompt 14 + `_comum.md` (§ Métricas do incremento) agora mandam a PR do passo 14 carregar o `append`.
- A PR do passo 14 (`sdd/aprendizados/*` + `sdd/metricas.csv`) sempre sai com `guard` vermelho ("mudança de produto sem incremento SDD afetado") — não está nas exclusões do `pre-merge-ci` (ao contrário de `sdd/historico/*`). Merge manual do dono, igual à #170 (03) e ao passo 14 do 02 (`c783a38`).

**How to apply:** conferir no GitHub o que o dono já mesclou antes de retomar.
